- content_for :title, "Sign in — WatchThis"

= stack({ gap: 6 }) do
  = prose do
    = h1("Sign in")
    = p do
      = "New to WatchThis? "
      = link_to("Create an account", new_user_registration_path)

  = form_with(model: resource, as: resource_name, url: session_path(resource_name)) do |f|
    = stack({ gap: 4 }) do
      %div.klods-field
        = f.label :email, "Email", class: "klods-label"
        = f.email_field :email, autofocus: true, autocomplete: "email", class: "klods-input"
      %div.klods-field
        = f.label :password, "Password", class: "klods-label"
        = f.password_field :password, autocomplete: "current-password", class: "klods-input"
      = cluster({ gap: 3 }) do
        = f.submit "Sign in", class: "klods-button klods-button--primary"
        = link_to "Forgot your password?", new_user_password_path, class: "klods-button klods-button--ghost"
