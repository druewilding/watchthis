- content_for :title, "Forgot your password? — WatchThis"

= stack({ gap: 6 }) do
  = prose do
    = h1("Forgot your password?")
    = p("Enter your email address and we will send you a link to reset it.")

  = form_with(model: resource, as: resource_name, url: password_path(resource_name)) do |f|
    = stack({ gap: 4 }) do
      = f.klods_field :email, label: "Email", type: :email, autofocus: true, autocomplete: "email"
      = cluster({ gap: 3 }) do
        = f.klods_submit "Send reset instructions"
        = link_to "Back to sign in", new_user_session_path, class: "klods-button klods-button--ghost"
